[project]
name = "ppt"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
  "pytest",
  "pydantic>=2",
  "ruamel.yaml>=0.18",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
